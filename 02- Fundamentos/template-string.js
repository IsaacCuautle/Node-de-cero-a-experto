const nombre = 'deadpool';
const real = 'wade winston';

const normal = nombre+ ' es '+ real;
// las comillas invertidas se activan con alt+96
const template = `${nombre} es ${real}`;

console.log(normal);
console.log(template);

const html = `<h1>Hola</h1>
<p>Mundo</p>`;

console.log(html);